@using Pharma.Entities;
@model Supplier

<h4>Delete</h4>

<h5>Are you sure you want to delete this?</h5>
<div>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @* @Html.DisplayNameFor(model=>model.Pr)*@
                    Supplier Name
                </th>
                <th>
                    Phone Number
                </th>
                <th>
                    Address
                </th>
                <th>
                    Email
                </th>

            </tr>
        </thead>
        <tbody>


            <tr>
                <td>
                    @* @Html.DisplayFor(modelItem=>item.SupplyName+" "+item.SupplyName)*@
                    @Model.Name
                </td>
                <td>
                    @* @Html.DisplayFor(modelItem=>item.SupplyType+" "+item.SupplyType)*@

                    @Model.Phone
                </td>
                <td>
                    @* @Html.DisplayFor(modelItem=>item.Manufacture)*@
                    @Model.Addrsss
                </td>
                <td>
                    @* @Html.DisplayFor(modelItem=>item.Manufacture)*@
                    @Model.Email
                </td>
                <td>
                    @*  <a asp-action="DeleteConfirm" type="submit" asp-route-id="@Model.id" class="btn btn-danger">Delete</a>*@
                    <form asp-action="DeleteConfirm">
                        <input type="hidden" asp-for="Id" />
                        <input type="submit" value="Delete" class="btn btn-danger" />
                        <a asp-action="Index" type="button" class="btn btn-primary">Cancel</a>
                    </form>
                </td>
            </tr>

        </tbody>
    </table>
